import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    searchBar: {
    borderStyle: 'solid',
    borderColor: 'black',
    borderWidth: 1.5,
    borderRadius: 15,
    padding: 5
  }
});

export default styles;